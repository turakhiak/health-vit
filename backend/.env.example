# Backend Environment Variables
PORT=8000
FRONTEND_URL=http://localhost:3000

# OpenAI (AI Coach & Vision)
OPENAI_API_KEY=sk-...

# Google Cloud (Drive & Fit)
# Save your service account or OAuth client secrets JSON content here if needed, 
# or use path to file. For OAuth web flow, we usually need:
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback/google
